../strategy/UI위젯_렌더링계약_20260819_1959.md

# Commit Report: WOT 요청 — IntroScene용 2D UI 위젯 렌더링 계약

## Summary

- 전략 문서 체크리스트 전 항목 완료.
- 신규 파일 15개(`ui/widgets/` 인터페이스 2 + 구현 6쌍, `tests/` 2), 기존 파일 13개 수정(`IRenderer.h`, DX9/11/12/OpenGL 렌더러, 3개 RendererTest, `CMakeLists.txt`).
- 빌드 + 전체 테스트 38/38 통과 (신규 11개 포함, 코드 리뷰에서 추가된 회귀 테스트 1개 포함).
- 코드 리뷰(`docs/review/UI위젯_렌더링계약_20260819_2030.md`)에서 발견된 [HIGH]/[LOW] 2건 즉시 해소.
- `docs/outbox/20260819_1932_wot_response.md` 응답 문서 작성 완료.

## Open Issues

- [MEDIUM] `ButtonUI`/`ImageUI`의 `onClick` 콜백이 실제 클릭으로 트리거되는지 검증하는 테스트가 없다. 현재 `UiWidgetsTest`는 생성 및 크래시 없음만 검증한다. ImGui 클릭 시뮬레이션(여러 프레임에 걸친 `ImGuiIO` 마우스 상태 조작) 테스트 헬퍼가 이 저장소에 아직 없어서, 이번 태스크 범위를 넘어서는 것으로 판단해 별도 처리로 분류했다(코드 리뷰 참고).

## 참고: WOT 쪽 후속 조치 필요

`World-of-Tank-imitation-Refactoring/CMakeLists.txt`의 `graphics_lib` 소스 목록을 수동 동기화해야 한다 — 이번에 추가된 `ui/widgets/*.cpp` 6개를 WOT 쪽 목록에도 추가해야 링크된다(`docs/outbox/20260819_1932_wot_response.md` 참고).
