../strategy/테스트인프라_잔여정리_20260811_2130.md

# Commit Report: 테스트 인프라 잔여 항목 정리

## 구현 결과 요약

- `graphics/CMakeLists.txt`의 11개 개별 `graphics_add_hlsl_shader(...)` 호출과 수동 `list(APPEND GRAPHICS_SHADER_OUTPUTS ...)` 블록을, `_graphics_shader_specs` 데이터 목록(`경로|entry|profile` 형식) + `foreach` 루프로 통합. 셰이더 추가/삭제 시 이제 데이터 목록 한 줄만 손대면 된다.
- 재구성/재빌드 결과 `.cso` 산출물 11개가 이전과 동일한 경로(`shaders/<backend>/*.cso`)에 생성됨을 확인.
- `ctest` 전체 재실행 — **28개 테스트 전부 Passed**, 회귀 없음.
- Code Review Stage 1에서 이슈 없음 확인.

## 발견된 이슈

없음 — 새로 추가/발견된 이슈 없음.

## 지난 사이클 DEFERRED 항목 처리 결과

- [RESOLVED] 11개 `graphics_add_hlsl_shader()` 호출의 반복 boilerplate.
  → resolved in: ../strategy/테스트인프라_잔여정리_20260811_2130.md (Phase 1)
- [재분류·종결] `OpenGLRenderer`에 `DirectX9/11/12RendererTest`와 대등한 전용 테스트가 없다는 지적.
  → 조사 결과 `OpenGLRenderer`(`renderer/opengl/OpenGLRenderer.h`/`.cpp`)는 의도적 스텁이다 — `Initialize()`는 인자를 무시하고 항상 `true`만 반환, `RenderFrame()`/`OnResize()`/`Shutdown()`은 완전한 no-op, `HandleUiMessage()`는 항상 `false`를 반환한다. 헤더 주석에도 "OpenGL 실사용은 이번 범위에 포함되지 않는다"고 명시되어 있다.
  → DirectX9/11/12Renderer처럼 실제 디바이스 초기화·렌더링 동작을 검증할 대상 자체가 없어, "대등한 커버리지"라는 원래 지적은 이 클래스에 적용되지 않는다고 판단해 **테스트 추가 없이 이슈를 재분류하고 종결**한다 (사용자 승인, 2026-08-11). 실제 OpenGL 구현에 착수하는 시점에 별도 task_delegation으로 테스트를 새로 작성하는 것을 권장한다.
