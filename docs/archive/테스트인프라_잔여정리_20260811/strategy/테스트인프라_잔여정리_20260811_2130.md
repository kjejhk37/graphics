../brainstorming/테스트인프라_잔여정리_20260811_2128.md

# Strategy: 테스트 인프라 잔여 항목 정리

## 확정된 방향 (브레인스토밍 결과 요약)

- 항목 1(셰이더 등록 boilerplate): `|` 구분자 데이터 목록 + `foreach` 루프로 11개 `graphics_add_hlsl_shader()` 호출을 통합한다.
- 항목 2(`OpenGLRenderer` 테스트): 코드는 변경하지 않는다. `OpenGLRenderer`가 의도적 스텁(Initialize/RenderFrame/OnResize/Shutdown/HandleUiMessage 전부 상수 반환/no-op)이라는 조사 결과에 따라, 리팩터 검사 이슈를 "DirectX와 대등한 커버리지 적용 대상이 아님"으로 재분류하고 사유를 커밋 리포트에 기록해 종결한다.

## How

### 항목 1

`graphics/CMakeLists.txt`의 기존 11개 `graphics_add_hlsl_shader(_graphics_dx9_baseline_vs "...")` 형태 호출과 그 뒤의 `list(APPEND GRAPHICS_SHADER_OUTPUTS ...)` 블록을, 브레인스토밍에서 검증한 아래 패턴으로 교체한다.

```cmake
set(_graphics_shader_specs
    "renderer/directx9/shaders/Baseline.vs.hlsl|VSMain|vs_3_0"
    "renderer/directx9/shaders/Baseline.ps.hlsl|PSMain|ps_3_0"
    "renderer/directx11/shaders/Baseline.vs.hlsl|VSMain|vs_5_0"
    "renderer/directx11/shaders/Baseline.ps.hlsl|PSMain|ps_5_0"
    "renderer/directx12/shaders/Baseline.vs.hlsl|VSMain|vs_5_0"
    "renderer/directx12/shaders/Baseline.ps.hlsl|PSMain|ps_5_0"
    "renderer/directx9/shaders/Instancing.vs.hlsl|VSMain|vs_3_0"
    "renderer/directx11/shaders/Instancing.vs.hlsl|VSMain|vs_5_0"
    "renderer/directx12/shaders/Instancing.vs.hlsl|VSMain|vs_5_0"
    "renderer/directx11/shaders/Transform.cs.hlsl|CSMain|cs_5_0"
    "renderer/directx12/shaders/Transform.cs.hlsl|CSMain|cs_5_0"
)
foreach(_spec ${_graphics_shader_specs})
    string(REPLACE "|" ";" _spec_fields "${_spec}")
    list(GET _spec_fields 0 _rel_source)
    list(GET _spec_fields 1 _entry_point)
    list(GET _spec_fields 2 _profile)
    graphics_add_hlsl_shader(_shader_output "${CMAKE_SOURCE_DIR}/${_rel_source}" ${_entry_point} ${_profile})
    list(APPEND GRAPHICS_SHADER_OUTPUTS ${_shader_output})
endforeach()
```

11개 개별 named 변수(`_graphics_dx9_baseline_vs` 등)는 더 이상 필요 없으므로 함께 제거한다.

### 항목 2

코드 변경 없음. Step 5 커밋 리포트에 재분류 사유를 기록한다.

## Checklist

### Phase 1 — 셰이더 등록 boilerplate 통합

- [x] `_graphics_shader_specs` 데이터 목록(11개 항목, `경로|entry|profile` 형식) 정의
- [x] `foreach` 루프로 `graphics_add_hlsl_shader()` 호출 + `GRAPHICS_SHADER_OUTPUTS` append를 대체
- [x] 기존 11개 개별 `graphics_add_hlsl_shader(...)` 호출 라인과 11개 named 변수, 수동 `list(APPEND ...)` 블록 제거
- [x] `cmake -S . -B build` 재구성 성공 확인
- [x] `cmake --build build --target shaders --config Debug` — 11개 `.cso` 산출물이 이전과 동일한 경로(`shaders/<backend>/*.cso`)에 생성되는지 확인 — 11개 전부 확인 (2026-08-11)
- [x] `cmake --build build --target tests --config Debug` 빌드 성공 확인
- [x] `ctest -C Debug` 실행 — 28개 테스트 전부 Passed 확인 (회귀 없음) (2026-08-11)

### Phase 2 — `OpenGLRenderer` 이슈 재분류

- [x] `docs/commit/`에 재분류 사유(의도적 스텁, DirectX와 대등한 커버리지 대상 아님) 기록

## 최종 승인 및 검증 기준

- Phase 1 체크리스트 전 항목 완료 + `ctest` 28개 전부 Passed.
- Phase 2는 코드 변경이 아니라 문서 기록이므로, 커밋 리포트에 재분류 사유가 명시되면 완료로 간주.
