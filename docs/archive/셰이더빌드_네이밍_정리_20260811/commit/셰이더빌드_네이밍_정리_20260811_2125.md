../strategy/셰이더빌드_네이밍_정리_20260811_2115.md

# Commit Report: 셰이더 빌드 파이프라인 네이밍 정리

## 구현 결과 요약

- `graphics/CMakeLists.txt`의 `wot_`/`WOT_` 접두어를 `graphics_`/`GRAPHICS_`로 전부 재명명 — 함수(`wot_add_hlsl_shader` → `graphics_add_hlsl_shader`), fxc 검색 임시 변수 4종, 셰이더별 임시 변수 11종, 리스트 변수(`WOT_SHADER_OUTPUTS` → `GRAPHICS_SHADER_OUTPUTS`), 컴파일 정의(`WOT_SHADER_OUTPUT_DIR` → `GRAPHICS_SHADER_OUTPUT_DIR`).
- `tests/ShaderBytecodeLoaderTest.cpp`의 `WOT_SHADER_OUTPUT_DIR` 참조도 `GRAPHICS_SHADER_OUTPUT_DIR`로 함께 수정 — "projects 원본과 1:1 동일 유지" 원칙에서 이 한 줄만 예외로 명시.
- 전략 문서 체크리스트 전 항목 `[x]` 완료.
- 재구성/재빌드/`ctest` 재실행 — **28개 테스트 전부 Passed**, 회귀 없음.
- 저장소 전체에 `wot_`/`WOT_` 식별자가 남아있지 않음을 grep으로 확인 (설명용 주석 2줄 제외).

## 발견된 이슈

없음 — Code Review Stage 1(순수 리네이밍, SOLID/라이브러리 격리/테스트/주석 규칙 모두 해당 없음 또는 위반 없음)에서 이슈 없이 통과. 리팩터 검사는 사용자 결정으로 생략.
