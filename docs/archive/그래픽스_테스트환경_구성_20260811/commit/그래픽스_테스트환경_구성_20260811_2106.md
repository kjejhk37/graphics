../strategy/그래픽스_테스트환경_구성_20260811_2043.md

# Commit Report: 그래픽스 단위 테스트 환경 구성

## 구현 결과 요약

- `graphics/CMakeLists.txt` 신규 작성 — `GRAPHICS_PLATFORM_DIR` 캐시 변수, FetchContent 5종(nlohmann_json/imgui/tinyobjloader/ufbx/cgltf), `platform_lib`/`graphics_lib` STATIC 라이브러리, `wot_add_hlsl_shader()` 셰이더 컴파일 파이프라인(fxc.exe), GoogleTest(v1.17.0) 연동.
- `graphics/tests/`에 projects `tests/`로부터 10개 테스트 파일(1단계 2개 + 2단계 8개)과 `cube.obj` 픽스처를 **복사**(원본은 그대로 유지).
- `graphics/CLAUDE.md` 신설 (`@agent_harness/CLAUDE.md` import).
- `graphics/.gitignore` 신설 (`build/` 제외).
- 전략 문서 체크리스트(Phase 0~5) 전 항목 `[x]` 완료.
- `ctest` 실행 결과: **28개 테스트 전부 Passed** (1단계 6개, 2단계 22개 — 실제 WARP/D3D 디바이스 초기화 포함).

## 발견된 이슈

- [RESOLVED] `wot_add_hlsl_shader()` 함수 주석이 CLAUDE.md Comment Writing Rules(Author/설명/Input/Output/Notes/Date)를 충족하지 않았음.
  → resolved in: ../strategy/그래픽스_테스트환경_구성_20260811_2043.md (Code Review Stage 1, `../review/그래픽스_테스트환경_구성_20260811_2058.md` 참고)
- [DEFERRED] 11개 `wot_add_hlsl_shader()` 호출이 backend/entry/profile만 다른 반복 형태 — 데이터 목록 + 반복문으로 통합하면 셰이더 추가/삭제 시 오타 위험을 줄일 수 있음.
  → 사용자가 다음 사이클로 미루기로 결정 (2026-08-11). 리팩터 검사 상세: ../review/그래픽스_테스트환경_구성_20260811_2058.md
- [DEFERRED] `wot_add_hlsl_shader()`/`_wot_*` 네이밍이 소비 프로젝트(WOT)의 컨벤션을 그대로 이식해왔지만 graphics 저장소 자체에는 'WOT' 개념이 없어 단독 관점에서 낯섦 — `graphics_add_hlsl_shader()` 등으로 재명명 검토.
  → 사용자가 다음 사이클로 미루기로 결정 (2026-08-11). 리팩터 검사 상세: ../review/그래픽스_테스트환경_구성_20260811_2058.md
- [DEFERRED] `OpenGLRenderer`는 `RendererFactoryTest`에서 타입 생성만 확인되고 `Initialize()`/`RenderFrame()` 동작을 검증하는 전용 테스트가 없음(projects 원본부터 있던 공백, 이번 사이클이 만든 문제는 아님).
  → 사용자가 다음 사이클로 미루기로 결정 (2026-08-11). 리팩터 검사 상세: ../review/그래픽스_테스트환경_구성_20260811_2058.md

## 참고 — 이번 사이클 범위 밖 (브레인스토밍에서 이미 합의됨)

- projects(`World-of-Tank-imitation-Refactoring`) `tests/`, `CMakeLists.txt`는 수정하지 않음(복사만 결정, ../brainstorming/그래픽스_테스트환경_구성_20260811_2035.md Q&A A2).
- `platform_lib`/`graphics_lib` 소스 목록 중복은 동기화 주석으로만 완화(Q&A A3) — 별도 도구 없음.
